# Copy to config/paths.env and edit.
# This file is sourced by run_all.sh and stage run scripts.

# Base project directory (will be created if missing)
BASE_DIR=/home/USER/mvGWAS_WMHv

# Genotype
# Provide either:
#  - GENO_INPUT: .vcf.gz / .vcf / .bcf / PLINK prefix (expects .bed/.bim/.fam)
#  - OR VCF_FILE (legacy)
GENO_INPUT=/path/to/geno.vcf.gz

# Legacy name (still supported)
VCF_FILE=/path/to/geno.vcf.gz

# Phenotypes and covariates (TSV with header)
PHENOTYPE_FILE=/path/to/WMH_phenotypes_complete.tsv
COVARIATE_FILE=/path/to/WMH_covariates_complete.tsv

# Nextflow pipeline directory (mvgwas-nf)
PIPELINE_DIR=/home/USER/mvgwas-nf

# Optional: comma-separated chromosomes (default: 1-22)
CHRS=1-22

# Sex coding in covariates
SEX_COL=sex
MALE_CODE=1
FEMALE_CODE=2

# Nextflow options
WITH_SINGULARITY=1          # 1=yes, 0=no
NEXTFLOW_RESUME=1           # 1=yes, 0=no
WINDOW_L=500                # mvgwas-nf --l
